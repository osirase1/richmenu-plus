{
  "name": "richmenu-plus-master-cloud",
  "version": "3.1.7-master-cloud-sheet-open-fix",
  "private": true,
  "description": "Master version of Rich Menu Plus with LINE API proxy, Apps Script cloud save, and checked clean file/DOM structure and direct spreadsheet open fix.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check api/*.js",
    "start": "vercel dev"
  }
}

{
  "name": "richmenu-plus-master-cloud",
  "version": "6.9.3-json-load-input-fix",
  "private": true,
  "description": "RichMenu Plus master cloud edition v6.9.3 with JSON project file input recovery fix.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check api/*.js",
    "start": "vercel dev"
  }
}
{
  "name": "richmenu-plus-master",
  "version": "3.1.1-master",
  "private": true,
  "description": "Master version of Rich Menu Plus for owner use with LINE API proxy and no app password screen.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check api/*.js",
    "start": "vercel dev"
  }
}

{
  "name": "richmenu-plus-master-cloud",
  "version": "6.0.0-master-cloud",
  "private": true,
  "description": "Master version of Rich Menu Plus with cloud save and visible spreadsheet link flow.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check api/*.js",
    "start": "vercel dev"
  }
}
